// Since the backend runs on this same Main PC, "localhost" is correct here.
// (The Thinkpad dashboard is the one that needs the real LAN IP — see dashboard/dashboard-config.js)
const ELLA_BACKEND_WS = 'ws://localhost:3001';
